# Replit — importe este repo e clique em Run (instale deps na primeira vez se pedir).
run = "streamlit run app.py --server.port=5000 --server.address=0.0.0.0 --server.headless=true"

entrypoint = "app.py"

[nix]
channel = "stable-23_11"
